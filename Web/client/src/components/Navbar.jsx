import React from "react";
import { Link } from "react-router-dom";


const Navbar = () => {
  const authUser=true;
  const loading=false;
  const handleLogout=(e)=>{
    e.preventDefault()
    // logout()
  }

  return };

export default Navbar;